name: easy_auto_form
description: Auto generate form from json schema
version: 0.0.2+3
homepage: https://github.com/CesareIsHere/easy_auto_form

environment:
  sdk: ">=3.0.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
